import { Route } from '@angular/router';
import { FeatureRegisterComponent } from './feature-register.component';

export const clientFeatureRegisterRoutes: Route[] = [
  { path: 'register', component: FeatureRegisterComponent },
];
